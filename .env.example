# --- Database (PostgreSQL with pgvector extension) ---
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_shop"

# --- Auth ---
AUTH_SECRET="generate-with: openssl rand -base64 32 (or see PowerShell note in README)"
AUTH_URL="http://localhost:3000"

# --- Anthropic (LLM for chat + reasoning over retrieved products) ---
ANTHROPIC_API_KEY="sk-ant-..."

# --- Embeddings (OpenAI used ONLY for text-embedding-3-small; swap for any embedding provider) ---
OPENAI_API_KEY="sk-..."

# --- Stripe ---
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# --- Email (Resend) ---
RESEND_API_KEY="re_..."
EMAIL_FROM="orders@yourstore.com"

# --- App ---
NEXT_PUBLIC_APP_URL="http://localhost:3000"
